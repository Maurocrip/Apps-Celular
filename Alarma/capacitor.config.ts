import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.Alarma',
  appName: 'Alarma',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
