import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'FotosEdificio.io',
  appName: 'Mi edificio',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  },
};

export default config;
